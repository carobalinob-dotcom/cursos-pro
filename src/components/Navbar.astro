---
// src/components/Navbar.astro
---

<!-- Barra de navegación principal -->
<nav class="bg-[#F7F3EF] shadow-sm">
  <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
    
    <!-- Logo Tipográfico -->
    <a href="/" class="text-3xl font-serif text-[#2B2B2B] tracking-wide">
      Rachel Studio
    </a>
    
    <!-- Botón de Menú (Hamburguesa) -->
    <button id="menu-open" class="text-slate-900 hover:text-[#A88C75] focus:outline-none transition-colors">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>
</nav>

<!-- Menú Pantalla Completa (Overlay) -->
<div id="fullscreen-menu" class="fixed inset-0 bg-white z-[60] opacity-0 pointer-events-none transition-opacity duration-300 flex flex-col">
  
  <!-- Cabecera del Menú -->
  <div class="px-6 h-16 flex items-center justify-between w-full max-w-6xl mx-auto mt-2">
    <!-- Logo dentro del menú -->
    <a href="/" class="text-2xl font-serif text-[#2B2B2B] tracking-wide">
      Rachel Studio
    </a>
    
    <!-- Botón de Cerrar (X) -->
    <button id="menu-close" class="text-slate-500 hover:text-slate-900 focus:outline-none transition-colors">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 font-light" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <!-- Enlaces del Menú (Centrados) -->
  <div class="flex-grow flex flex-col items-center justify-center space-y-8 pb-20">
    <a href="/" class="text-xl text-[#A88C75] hover:text-[#2B2B2B] font-serif tracking-widest uppercase transition-colors">Inicio</a>
    <a href="/talleres" class="text-xl text-[#A88C75] hover:text-[#2B2B2B] font-serif tracking-widest uppercase transition-colors">Talleres</a>
    <a href="/tutoriales" class="text-xl text-[#A88C75] hover:text-[#2B2B2B] font-serif tracking-widest uppercase transition-colors">Tutoriales</a>
    <a href="/productos" class="text-xl text-[#A88C75] hover:text-[#2B2B2B] font-serif tracking-widest uppercase transition-colors">Productos</a>
    <a href="/contacto" class="text-xl text-[#A88C75] hover:text-[#2B2B2B] font-serif tracking-widest uppercase transition-colors">Contacto</a>
  </div>
</div>

<!-- Lógica de interactividad -->
<script>
  const menuOpen = document.getElementById('menu-open');
  const menuClose = document.getElementById('menu-close');
  const fullscreenMenu = document.getElementById('fullscreen-menu');

  // El 'if' le asegura al editor que los elementos sí existen, quitando el error rojo
  if (menuOpen && menuClose && fullscreenMenu) {
    
    menuOpen.addEventListener('click', () => {
      fullscreenMenu.classList.remove('opacity-0', 'pointer-events-none');
      fullscreenMenu.classList.add('opacity-100', 'pointer-events-auto');
      document.body.style.overflow = 'hidden'; 
    });

    menuClose.addEventListener('click', () => {
      fullscreenMenu.classList.remove('opacity-100', 'pointer-events-auto');
      fullscreenMenu.classList.add('opacity-0', 'pointer-events-none');
      document.body.style.overflow = ''; 
    });
    
  }
</script>